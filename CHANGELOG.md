# Changelog

## Unreleased

- Build: stop emitting the unused `templates/assets/js/main_js.js` bundle and clean stale copies during build.
- Post page: avoid duplicate code-copy buttons, add `Esc` support for the image lightbox, and keep post-specific behavior isolated from global copy logic.
- Plugin adapter: support multiple plugin containers, defer initialization until the container is near the viewport, and cache identical API requests.
- Maintenance: remove temporary leftovers from the workspace and keep fix tracking in `FIX-TRACKING.md`.

## Legacy History

- Historical release notes remain under `docs/legacy/`.
- Older version-specific changelogs are preserved there until the new top-level changelog is fully backfilled.
